<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/HR.css">
    <title>Fájl feltöltés</title>
</head>
<body>
    <header class="d-flex flex-wrap justify-content-center py-3 border-bottom container-fluid header-color header">
        <a href="/homepage" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto text-dark text-decoration-none">
            <img src="icon-1.png" width="55" height="70" class="p-2"/>
            <span class="text-color">Törzsadatbázis</span>
        </a>
        <div class="text-end">
            <button type="button" class="btn-1 text-color">Felhasználó</button>
        </div>
    </header>
</head>
<body>

    <div class="grid-container">
        <!-- Left column: file upload form -->
        <div class="hero-container">
            <h1>Fájl feltöltés (xlsx)</h1>
            <form action="/uploadfile" method="POST" enctype="multipart/form-data">
                <label for="customFileName">Írja be a tábla nevét:</label>
                <input type="text" id="customFileName" name="customFileName" placeholder="Tábla neve">
                <br><br>
                <input type="file" name="file" accept=".xlsx" required>
                <br><br>
                <button class="btn btn-success" type="submit">Feltöltés</button>
            </form>
            <br>
            <h3>Frissítés esetén válasszon a meglévő táblák közül:</h3>
            <br>
            <% tableNames.forEach(tableName => { %>
                <button class="table-button" onclick="fillTableName('<%= tableName %>')"><%= tableName %></button>
            <% }); %>
        </div>
    </div>
    
    <script>
        // Function to fill the input with the selected table name
        function fillTableName(tableName) {
            document.getElementById('customFileName').value = tableName;
        }
    </script>

    <!-- <div class="footer-container">
        <% const date = new Date(); %>
        <% const year = date.getFullYear(); %>
        <p>&copy; <%= year %> Minden jog fenntartva.</p>
    </div> -->

</body>
</html>
